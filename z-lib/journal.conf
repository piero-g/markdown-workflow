# See README before using this config file:
# accepted values are [A-Za-z0-9]*
journal_shortname=demo
# accepted values are [A-Za-z0-9/]*
backup_path=
# numbered Sections in articles (uncomment to disable section numbering)
#unnumbered_sections=true
